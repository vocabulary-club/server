<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.server.api.mapper.UserMapper">

	<select id="selectByUsername" resultType="web.server.api.entity.UserEntity" parameterType="String">
        SELECT * FROM vc_user WHERE username = #{username};
    </select>

    <insert id="insert" parameterType="web.server.api.entity.UserEntity">
        INSERT INTO shineug.vc_user (provider, username, password, name, email, role, picture, verified)
        VALUES (#{provider}, #{username}, #{password}, #{name}, #{email}, #{role}, #{picture}, #{verified});
    </insert>

    <update id="update" parameterType="web.server.api.entity.UserEntity">
        UPDATE shineug.vc_user
        SET name = #{name}, picture = #{picture}, updated_at = NOW()
        WHERE username = #{username};
    </update>

    <delete id="deleteByUsername" parameterType="String">
        DELETE FROM shineug.vc_user
        WHERE username = #{username};
    </delete>

    <select id="existsByUsername" resultType="int" parameterType="web.server.api.entity.UserEntity">
        SELECT COUNT(1)
        FROM shineug.vc_user
        WHERE provider = #{provider} and username = #{username}
    </select>

    <update id="verify" parameterType="String">
        UPDATE shineug.vc_user
        SET verified = 'Y'
        WHERE username = #{username};
    </update>
    
</mapper>